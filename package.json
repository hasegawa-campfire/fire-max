{
  "name": "fire-max",
  "version": "1.3.5",
  "source": "src/index.html",
  "license": "MIT",
  "scripts": {
    "serve": "serve -L -p 3000 src",
    "prebuild": "rm -rf dist",
    "build": "node build.mjs",
    "preview": "serve -L -p 8080 dist"
  },
  "devDependencies": {
    "@craftamap/esbuild-plugin-html": "^0.6.1",
    "esbuild": "^0.19.2",
    "esbuild-plugin-copy": "^2.1.1",
    "glob": "^10.3.10",
    "serve": "^14.2.1"
  },
  "dependencies": {
    "html-minifier": "^4.0.0"
  }
}
